# docs

* `how_it_works.html` — the explainer page (pipeline diagrams, step-by-step, tools inventory, build log with proof). Open it in a browser.
* `proof/` — screenshots referenced by the page (brain snapshots, the M8M mesh measurement).
* `audio/` — riffs the fly made up from a 19-tab corpus (`preview_djent_*.mp3`). No copyrighted recordings.
* `controls.json` — the control experiments: real wiring vs shuffled vs random reservoirs (`python -m flybrain_composer.cli controls`).
